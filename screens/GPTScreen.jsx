import {View, Text} from "react-native";
export default function GPTScreen() {
    return (
        <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
            <Text style={{fontSize: 30}}>GPT Screen</Text>
        </View>
    );
}